# Use the latest 2.1 version of CircleCI pipeline processing engine, see https://circleci.com/docs/2.0/configuration-reference/
version: 2.1
jobs:
  build:
    environment:
      VERSION: 0.0.1
      GO111MODULE: "on"
    docker:
      - image: golang:latest
    working_directory: /go/src/github.com/mschurenko/docker-cloner
    steps:
      - checkout
      - run: mkdir ./workspace
      - run: go build -ldflags "-X main.version=$VERSION" -o docker-cloner
      - run: cp -v ./docker-cloner ./workspace
      - persist_to_workspace:
          root: workspace
          paths:
            - docker-cloner
  test:
    docker:
      - image: docker:18.09.1-git
    steps:
      - attach_workspace:
          at: ./artifacts
      - run: ls -l ./artifacts
      - run: ls -l ./artifacts/docker-cloner

  # publish-github-release:

# Orchestrate or schedule a set of jobs, see https://circleci.com/docs/2.0/workflows/
workflows:
  main:
    jobs:
      - build
      - test:
          requires:
            - build
