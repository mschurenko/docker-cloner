# Use the latest 2.1 version of CircleCI pipeline processing engine, see https://circleci.com/docs/2.0/configuration-reference/
version: 2.1
jobs:
  build:
    environment:
      VERSION: 0.0.1
      GO111MODULE: "on"
    docker:
      - image: golang:latest
    working_directory: /go/src/github.com/mschurenko/docker-cloner
    steps:
      - checkout
      - run: mkdir ./workspace
      - run: GOOS=linux go build -ldflags "-X main.version=$VERSION" -o workspace/docker-cloner
      - persist_to_workspace:
          root: workspace
          paths:
            - docker-cloner
  test:
    docker:
      - image: docker:18.09.1-git
      # - image: circleci/python:latest
    steps:
      - setup_remote_docker
      - attach_workspace:
          at: ./artifacts
      - run: ls -l ./artifacts
      - run: echo $PATH
      - run: pwd
      - run: ls
      - run: docker run --rm -d --name test alpine sleep 120
      - run: >
          /root/project/artifacts/docker-cloner
          -memory 200
          -id test
          -cmd "echo In copy contianer. PASSED"
      # - run: artifacts/docker-cloner -version

  # publish-github-release:

# Orchestrate or schedule a set of jobs, see https://circleci.com/docs/2.0/workflows/
workflows:
  main:
    jobs:
      - build
      - test:
          requires:
            - build
